# Pins the rendered app name to `kache`, which is what every resource and —
# critically — every selector label already uses in the clusters running this
# chart. Selector labels are immutable on a Deployment, so letting the rendered
# name follow the chart rename below would make the next upgrade fail rather
# than roll.
nameOverride: "kache"
fullnameOverride: ""

replicaCount: 1

# Deployment update strategy, honoured only when
# `planner.persistence.type` is `ephemeral`.
#
# With `pvc` the chart forces `Recreate` and ignores this: the planner DB is
# single-writer, so any surge deadlocks the rollout permanently (see the
# comment in templates/deployment.yaml). Empty means the Kubernetes default.
updateStrategy: {}

image:
  repository: zondax/kache
  tag: ""
  pullPolicy: IfNotPresent

imagePullSecrets: []

planner:
  name: planner
  dbPath: /var/lib/kache/planner.db
  seedStateFile: ""
  persistence:
    enabled: true
    type: ephemeral
    mountPath: /var/lib/kache
    size: 10Gi
    storageClassName: ""
    accessModes:
      - ReadWriteOnce

auth:
  # Static shared token (`KACHE_PLANNER_TOKEN`) from an existing Secret.
  existingSecret: ""
  existingSecretKey: token
  # People log in with `kache login` against this issuer. The client id is
  # the accepted token audience and is advertised at /.well-known/kunobi-auth.
  oidc:
    issuer: ""
    clientId: ""
  # GitHub Actions ID tokens issued for this audience, accepted only from
  # these repository owners. Jobs need `permissions: id-token: write`. The
  # kache client requests the planner's base URL as audience by default, so
  # set this to the URL clients use (e.g. the Ingress host).
  githubOidc:
    audience: ""
    owners: []

ha:
  enabled: false
  leaseName: kache-service
  namespace: ""
  serviceAccount:
    create: true
    name: ""

logLevel: "kache_service=info"

service:
  type: ClusterIP
  port: 8080
  # Protocol an Ingress uses to reach the pod. Build sessions are one HTTP/2
  # stream per build, so behind an Ingress set `kubernetes.io/h2c`; the
  # service speaks HTTP/1.1 and HTTP/2 on the same port.
  appProtocol: ""
  annotations: {}

# Expose the service through an Ingress. Set `host` when enabled. Add
# `cert-manager.io/cluster-issuer` to `annotations` to have cert-manager issue
# the TLS secret.
ingress:
  enabled: false
  className: ""
  host: ""
  path: /
  pathType: Prefix
  annotations: {}
  tls:
    enabled: false
    # Defaults to <fullname>-tls.
    secretName: ""

podAnnotations: {}
podLabels: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 65532
  runAsGroup: 65532
  fsGroup: 65532
  fsGroupChangePolicy: OnRootMismatch
  seccompProfile:
    type: RuntimeDefault

containerSecurityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 128Mi

extraEnv: []

nodeSelector: {}
tolerations: []
affinity: {}

# SigNoz metrics scraping (how the planner's Prometheus metrics reach SigNoz).
#
# The cluster's SigNoz OTel collector discovers scrape targets by pod
# annotations (signoz.io/scrape|port|path) — NOT by Prometheus ServiceMonitors,
# because these clusters run no Prometheus to consume them. The planner always
# serves /metrics on the http port (auth-exempt, like the health probes), so
# this is ON by default; the annotations are inert on any cluster without a
# SigNoz collector watching for them.
signoz:
  scrape: true
  path: /metrics
  # port: defaults to service.port (the http port serving /metrics)
