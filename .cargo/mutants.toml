# Keep mutation runs focused on semantic failures rather than lint or copied
# scratch-data noise. Scope, baseline, and time limits stay explicit per lane.
cap_lints = true
gitignore = true

# Two independent reasons a mutant here is unobservable rather than untested.
#
# `run_config_editor` and `run_monitor` are TUI entry points: each enables raw
# mode, takes over the alternate screen, and runs an event loop until the user
# quits. Nothing can drive those functions from a test — CI has no TTY at all,
# and a test that did get one would seize the developer's terminal and then
# block — so a replaced body is unobservable and survives by construction
# rather than from a coverage gap. Kept as narrow as possible: the pure state,
# scheduling, scan, and rendering helpers called by both entry points stay in
# scope and are mutation-tested normally.
#
# The mutation lane runs on ubuntu-latest, so a mutant inside code compiled only
# for another platform is never built and therefore always "survives" — that is
# a property of where the lane runs, not a gap in the tests. `probe_macos` is
# `#[cfg(target_os = "macos")]` and `probe_unsupported` covers the remaining
# targets; both are unreachable there.
#
# Kept as narrow as the names allow: `probe_linux` — the one impl the lane does
# compile — stays fully in scope, along with `fiemap_window_length`,
# `Sharing::unknown_for`, and every caller. macOS coverage comes from the
# module's own tests, which do run on the macOS arm of `Test (macOS)`.

#
# `AttrList` is listed separately because a struct-field mutant's name does not
# carry the enclosing function, so "probe_macos" alone does not match it. The
# struct is declared inside `probe_macos` and exists nowhere else, so naming it
# is exactly as narrow.
#
# C/C++ output independence has three cfg-selected implementations. The Linux
# lane exercises the Unix implementation; the Windows body is covered by the
# Windows lane, while unsupported targets deliberately use a constant fallback.
#
# Kani proof harnesses are compiled and executed only by `cargo kani`; the
# mutation lane's `cargo test` process cannot observe changes under `cfg(kani)`.
# Their production target remains mutation-tested, while the harnesses are
# independently gated by the Kani proofs job.
#
# `run_gc_local` (CLI) and `Daemon::run_gc` gate progress strings and info
# logs on `count > 0` comparisons — "N entries updated." vs "up to date.",
# and whether a tracing line fires. Every `>` in both functions today is such
# a gate: which cosmetic string prints is unobservable from `cargo test`
# (stdout of the spawned CLI, daemon tracing output), while the counts
# themselves come from `Store::backfill_*` / eviction, which are
# mutation-tested directly. The `+` in `Daemon::run_gc` is the same shape:
# it sums two cohort counts purely to decide whether the shadow-demand info
# line prints (#594); the counts come from `shadow_demand_split`, which is
# tested directly. Revisit if either function ever grows a comparison or sum
# with semantic weight.
#
# `join_inflight_download` compares `Instant::now() < deadline` to decide
# whether a waiter may still adopt a newer leader generation. Flipping `<` to
# `<=` moves the boundary by one unmeasurable instant: distinguishing them
# requires the comparison to run at exactly the deadline nanosecond, which no
# test can arrange. The meaningful mutations of the same comparison (`==`,
# `>`, and the surrounding branch logic) are killed by the generation-adoption
# and give-up tests.
# `MacOsPlatform::package_debug_bundle` past its host gate is observable only
# where `dsymutil` runs: the macOS Test lane exercises it against a real
# binary (bundle produced, tar contents, determinism), but this lane is
# ubuntu, where the method returns None before any of that code executes —
# the probe_macos situation again, scoped to the one method. Its pure
# helpers (build_deterministic_tar, collect_files_recursively) are NOT
# excluded; Linux-runnable tests cover them.
#
# `default_cache_executables` is a per-platform constant: on this lane the
# real value IS true, so the with-true replacement is value-identical here
# (the with-false variant is caught); the macOS/Windows Test lanes pin
# their expected values.
#
# `touch_mtime_write_clock` ORs together disjoint open(2) flag bits, and
# `O_RDONLY` is zero: for disjoint operands `|` and `^` compute identical
# values, so those mutants are equivalent by construction, not untested.
# The `&` variants are excluded for lane economics, not lack of coverage:
# dropping O_NOFOLLOW is killed by the symlink-refusal test and dropping
# O_NONBLOCK by the FIFO-refusal test — but the latter's only observable
# effect is that the open BLOCKS, so the lane can only "kill" it by burning
# its full 300s timeout, which cargo-mutants then reports as a timeout
# failure rather than a catch. The tests remain the enforcement. The
# fd-validity comparison `raw < 0` mutated to `<= 0` misclassifies only
# fd 0, which `open` returns solely when stdin is closed — no test can
# arrange that without racing the whole test process's stdio; the `==` and
# `>` mutations of the same comparison are killed.
#
# `sample_write_clock` is `#[cfg(not(unix))]`: the same platform-lane
# property as `probe_macos` above — the ubuntu lane never compiles it, so
# every mutant inside it "survives" by construction. Its behavior is
# exercised by the Windows arm of `Test (Windows)` (the write-clock
# ordering test) and by the e2e lifecycle on the Windows runner.
#
# The two `windows_*` path adapters only translate host `Path` components into
# platform-neutral classifiers. Ubuntu cannot compile their Windows bodies;
# the classifiers remain in scope and have exhaustive Linux-runnable tests.
#
# `windows_kache_processes` is `#[cfg(windows)]`: the `sample_write_clock`
# property again — the ubuntu lane never compiles it, so every mutant inside
# it survives by construction. Kept as narrow as the name allows: the decision
# it feeds, `cmdline_is_daemon_run`, is deliberately `#[cfg(any(windows, test))]`
# precisely so this lane still mutation-tests the matching rule that decides
# which processes force_recover may kill.
#
# The two operator mutants inside `find_daemon_pids` are the same story: both
# live in its `#[cfg(windows)]` arm — `*pid != own_pid && cmdline_is_daemon_run(..)`
# — which this lane does not compile. Its Unix arm is untouched by the change
# that introduced these entries, so nothing here narrows coverage of the code
# the lane can actually reach. The function's return value stays fully in
# scope and is killed by tests asserting both directions: a process running a
# `kache` executable is found, and a shell whose command line merely mentions
# `kache daemon run` is not.
#
# The clean reclaim scanner has the same narrow platform boundary. The Windows
# syscall adapter and the unsupported-target wrapper are not compiled by the
# Ubuntu mutation lane, so replacement mutants inside those wrappers cannot be
# observed there. Their logic is deliberately extracted into the platform-neutral
# `windows_file_identity_from_parts`, `windows_storage_observation`, and
# `unsupported_storage_observation` helpers, which remain mutation-tested on
# Linux. Hosted Windows tests still exercise the real identity syscall, stable
# identity, link count, and failure behavior. Only the cfg-only adapters below
# are excluded.
#
# `run_cargo_non_unix` is the process-status half of the guarded Cargo
# front-end. The Ubuntu mutation lane compiles only the Unix `exec` half;
# hosted Windows tests still compile the status/exit-code path.
#
# `report_key_divergence` is a two-line stderr printer for the e2e convergence
# diagnostic. Its only effect is bytes on the e2e driver's stderr, which an
# in-process test cannot observe. Kept as narrow as the name allows: every
# decision it delegates to stays in scope and is killed by direct unit tests —
# `parse_keytrace` (which crate and field a trace line names),
# `render_key_divergence` (which fields count as diverged), and
# `key_divergence_report` (reading both phase logs off disk).
#
# In `install_shims_named_with_output`, `!verbose` and the summary guards
# (`!created.is_empty()`, `!skipped.is_empty()`, `!marked`) decide only what
# prints: the created and skipped name lists and the unmarked-directory note.
# Nothing an in-process test can observe changes: the shim set it creates, the
# entries it preserves, and the errors it reports are all asserted directly by
# the tests around it. Same shape as the `run_gc_local` progress strings above.
# The pattern also matches the function's other `!` guards (`--force`, the
# extra-name checks), which those tests kill. Whether the `.kache-shims`
# marker is written is decided in `compiler::shim::holds_only_shims`, which
# the pattern does not match, so that decision stays in scope.
#
# Replacing `Config::socket_path` with an empty `PathBuf` is not a survivor:
# `test_config_socket_path` and the runtime-isolation tests reject it directly.
# Other daemon lifecycle tests then try to bind/connect the empty path and block
# until cargo-mutants' 180s process timeout, so the lane reports a timeout rather
# than the already-observed assertion failure. Exclude only that whole-body
# replacement; the override resolver and every branch inside the real method
# remain mutation-tested.
#
# Replacing `try_collect_slots` with `Ok(None)` makes every permit unavailable.
# The wrapper then waits for its 30-minute production deadline, beyond the
# mutation lane 180-second timeout. Short-timeout scheduler tests cover permit
# blocking and release; exclude only this whole-body replacement.
#
# The packed-prefetch format has three inclusive-boundary operator mutants
# that cannot change an observable result. A catalog which passed structural
# validation cannot approach the 64 MiB encode cap: its 65,536 fixed-shape
# entries, 16,384 pack wrappers, and two 4 KiB selector fields remain far
# below it. On decode, an object of exactly the 16-byte header length has no
# JSON index/payload and is rejected either way; likewise an index ending at
# EOF has no payload for the required non-empty, non-zero-length entry set.
# Exact-limit and first-real-overshoot tests retain coverage of every reachable
# boundary on the corresponding decode/build paths.
#
# The benchmark entry points orchestrate real repository clones, compilers,
# cache daemons, and multi-gigabyte scenarios. Their whole-body `Ok(())`
# replacement cannot be observed by the hermetic `cargo test` process. The
# Linux/macOS/Windows E2E smoke lanes exercise those entry points instead.
# Keep only the whole-body replacements out of this lane: their parsing,
# selection, measurement, verdict, artifact, and OTLP helpers remain in scope.
# `run_cold_phase` is the same shape: a full clone/build that this lane cannot
# drive. The dump it now calls (`write_cache_otlp_or_warn`) stays in scope. The
# mbx arm's entry point is the same shape as the sccache one, and
# `print_mbx_summary` writes the human summary to the e2e driver's
# stderr, the `report_key_divergence` shape above: an in-process test cannot
# observe those bytes. Every number it prints is computed by helpers that are
# tested directly (`external_measure_warnings`, `phase_speedup`, `disk_delta`,
# `bytes_to_mib`, `MbxPhaseMetrics::from_report`).
#
# macOS SDK discovery, `macos_oso_prefix_flag` and `oso_prefix_root_for_key`
# are compiled only on macOS, while the diff mutation lane runs on Ubuntu, and
# the other platforms' `oso_prefix_root_for_key` is a `None` stub. macOS tests
# exercise SDK discovery and the real wrappers. Exclude only the platform
# wrappers; the extracted flag logic, `oso_prefix_root_for_key_inner` and key
# folding remain mutation-tested on every host.
#
# `cache_fs` also has thin macOS and Windows syscall wrappers which this Ubuntu
# lane cannot compile. Their sizing and validation rules are extracted into
# platform-neutral helpers and stay mutation-tested here; hosted platform tests
# exercise the real wrappers.
#
# Installed MSVC discovery is a Windows-only adapter around find-msvc-tools.
# The Ubuntu mutation lane cannot compile that body, while replacing the
# non-Windows stub with `Ok(())` is value-identical. Keep only that whole-body
# replacement out of this lane. Cross-platform tests cover the environment
# guard and composition helpers; the hosted Windows test runs the real adapter
# without requiring a developer prompt.
 #
# The service repository has no identity-manifest store yet, so its
# `identity_candidates` implementation returns `Ok(Vec::new())`. Replacing that
# with `Ok(vec![])` is the same value. The daemon and core planner identity paths
# remain in scope and are exercised with populated manifests.
#
# The Windows copy-restore warning adapter calls volume APIs and emits console
# advice. Linux does not compile it; its portable classification helpers remain
# in mutation scope.
#
# kache-fs keeps native APFS queries in a macOS-only module and names the other
# platform adapters explicitly. The Ubuntu mutation lane cannot compile those
# adapters; macOS and Windows tests exercise them. The portable APFS allocation
# classifier, extent walker and Windows identity assembly stay in scope.
# TempDir is fixture setup and cleanup, exposed by the testing feature for
# consumer tests; mutating it tests the harness instead of filesystem behavior.
# windows_identity combines a shifted u32 with an unshifted u32. Their bits do
# not overlap, so replacing OR with XOR produces the same u64 for every input.
#
# `print_pull_summary` and `print_sccache_summary` are `print_mbx_summary`'s
# shape: they only write the human summary to the e2e driver's stderr. The
# numbers they print come from `disk_usage::measure`, `bytes_to_mib` and the
# phase parsers, which are tested directly.
#
# `disk_usage::other` is the non-Unix fallback for inode identity and
# allocated size. The ubuntu lane never compiles it; `disk_usage::unix` and
# the measurement walk stay in scope.
#
# `bench_host::macos` and `bench_host::other` are the same shape: thin
# wrappers around `sysctl` and `mount`, and a constant fallback, that the
# ubuntu lane never compiles. Their parsers and the procfs readers are
# platform-neutral and stay in scope; the macOS Test lane runs the real
# wrappers through the host smoke test.
# The sccache preflight adapter calls Darwin-only process and sandbox APIs.
# Its decision logic lives in src/fallback/sandbox.rs and stays in this Linux
# lane. Test (macOS) covers the native adapter with allowed/denied subprocesses.
exclude_globs = ["crates/kache-fs/src/apfs.rs", "src/fallback/macos.rs"]
exclude_re = [
    # `session_lock` and `advertised_login` compare an elapsed time against a
    # bound with `<`; `<` versus `<=` differs only when the elapsed time equals
    # the bound to the nanosecond, which no test can arrange. Both sides of
    # each bound are tested.
    "replace < with <= in (session_lock|advertised_login)",
    "crates/kache-fs/src/copy.rs:.*(native_reflink_macos|native_reflink_windows|native_reflink_unsupported|windows_cluster_size)",
    "crates/kache-fs/src/identity.rs:.*windows_metadata_identity",
    "crates/kache-fs/src/identity.rs:.*windows_handle_identity",
    "crates/kache-fs/src/identity.rs:.*replace \\| with \\^ in windows_identity",
    "crates/kache-fs/src/lib.rs:.*testutil::",
    "replace run_config_editor",
    "replace run_monitor",
    # `run_monitor` keeps one check of its own: refusing to start when stdout
    # is not a terminal. A test cannot flip that without either seizing the
    # developer's terminal or running under a pseudo-terminal the lane does
    # not have; the loop body it guards is exercised through the extracted
    # `ingest_tailed_records`, `stats_fetch_due`, `project_scan_due`, and
    # `handle_terminal_event`, which stay in scope.
    "delete ! in run_monitor",
    "probe_macos",
    "replace sdk_identity_for",
    "xcrun",
    "replace macos_oso_prefix_flag ->",
    "replace oso_prefix_root_for_key ->",
    "probe_unsupported",
    "from struct AttrList",
    "regular_output_is_independent_windows",
    "regular_output_is_independent_unsupported",
    "kani_proofs::",
    "replace > with (>=|<|==) in run_gc_local",
    "replace > with (>=|<|==) in Daemon::run_gc",
    "replace \\+ with . in Daemon::run_gc",
    "replace < with <= in join_inflight_download",
    # `capture_rustc_stderr` reads until end of file, and that comparison is
    # the loop's only exit. Inverted, the reader spins on the zero-length read
    # that means EOF, so the lane reports a timeout: there is no test that can
    # observe a loop which never finishes. Every other mutation of this
    # function is tested — the delivered, undelivered and verbatim bytes are
    # all asserted.
    "replace != with == in capture_rustc_stderr",
    "MacOsPlatform>::package_debug_bundle",
    "replace default_cache_executables -> bool with true",
    "replace \\| with \\^ in touch_mtime_write_clock",
    "replace \\| with & in touch_mtime_write_clock",
    "replace < with <= in touch_mtime_write_clock",
    "sample_write_clock",
    "NonInheritableStdio",
    "windows_pattern_has_ambiguous_root",
    "windows_path_uses_device_namespace",
    "windows_kache_processes",
    "replace && with \\|\\| in find_daemon_pids",
    "replace != with == in find_daemon_pids",
    "query_windows_file_identity",
    # The Windows half of `fsync_published_blob`: a published blob is
    # read-only and Windows needs write access to flush a handle, so the
    # attribute comes off and goes back on around the flush. That block is
    # `#[cfg(windows)]`, never compiled in this Linux lane, so a mutant
    # inside it changes nothing that runs here.
    "delete ! in fsync_published_blob",
    "observe_storage_windows",
    "observe_storage_unsupported",
    "run_cargo_non_unix",
    "replace report_key_divergence with \\(\\)",
    "delete ! in install_shims",
    "replace Config::socket_path .* with Default::default",
    "replace try_collect_slots .* with Ok\\(None\\)",
    "src/remote_pack.rs:351:20: replace > with (>=|==) in encode_catalog",
    "src/remote_pack.rs:309:20: replace < with <= in decode_pack",
    "src/remote_pack.rs:324:22: replace > with >= in decode_pack",
    "replace run_bench .* with Ok",
    "replace run_pull_bench .* with Ok",
    "replace run_sccache_bench .* with Ok",
    "replace run_mbx_bench .* with Ok",
    "replace print_mbx_summary with \\(\\)",
    "replace print_pull_summary with \\(\\)",
    "replace print_sccache_summary with \\(\\)",
    "crates/kache-e2e/src/disk_usage.rs:.*other::",
    "crates/kache-e2e/src/bench_host.rs:.*(macos|other)::",
    # The test runner's Windows console adapter, the `disk_usage::other`
    # shape: the ubuntu lane never compiles it. The Unix `signals` module
    # stays in scope, and the runner's integration tests kill its mutants.
    "src/test_runner.rs:.*windows_signals::",
    "replace run_cold_phase .* with Ok",
    "src/cache_fs.rs:.*replace probe_windows",
    "src/cache_fs.rs:.*replace statfs_total_bytes_macos",
    "src/cache_fs.rs:.*replace volume_total_bytes_windows",
    "src/native_link_key.rs:.*replace WindowsProbeEnvironment::augment_from_installed_msvc .* with Ok",
    "SqlitePlannerRepository>::identity_candidates .* with Ok\\(vec!\\[\\]\\)",
    "crates/kache-store/src/link.rs:.*replace warn_no_cow_restore_once with \\(\\)",
    # Build-script execution cache. The non-Unix `install` is a stub that
    # refuses (`run_cargo_non_unix` shape: the ubuntu lane compiles only the
    # Unix body, whose launcher tests stay in scope). `pin_kache` copies the
    # binary only after `hard_link` fails, which one filesystem cannot
    # arrange, so the guard on its race fallback is unreachable here.
    # `tree_stamp`'s `>` keeps the newest mtime: replacing it with `>=`
    # reassigns an equal value, the same result. `Run::restore` sums sizes
    # for the event's `size` field only; every restored byte is asserted by
    # the round-trip tests, the sum is not a decision.
    "src/build_script.rs:.*replace install -> Result<\\(\\)> with Ok\\(\\(\\)\\)$",
    "replace match guard pinned.is_file\\(\\) with (true|false) in pin_kache",
    "replace > with >= in tree_stamp",
    "replace \\+= with \\*= in Run::restore",
    # Lock-poll back-off. `attempt` only paces the sleep between polls
    # (`lock_poll_interval(attempt)`, capped at 100 ms); `*=` leaves it at
    # the first interval, which changes how often a waiter polls and nothing
    # a test can observe. The interval function itself is tested directly.
    "replace \\+= with \\*= in StoreLock::wait_until_available",
    "replace \\+= with \\*= in Scheduler::wait_for_permit",
    "replace \\+= with \\*= in wait_for_lock",
    "replace \\+= with \\*= in acquire_within",
    # Printing rustc's captured stdout/stderr is gated on `!is_empty()`, so
    # dropping the gate prints an empty string: a no-op.
    "delete ! in run_parsed_rustc",
    # `daemon_local && precompiled.is_none()`: without a daemon socket
    # `try_daemon_local_hit` returns None, so the flip is observable only
    # with a live local-hit daemon, which the e2e lanes run. The precompiled
    # half is covered by the passthrough exit-code test.
    "src/wrapper.rs:.*replace && with \\|\\| in run_parsed_rustc",
    # `verify` gates one "Repairing:" line on `removed + inputs > 0`, the
    # `run_gc_local` shape above; `prune_cc_preprocess_memos` is tested
    # directly.
    "replace match guard removed \\+ inputs > 0 with (true|false) in verify",
    "replace \\+ with . in verify",
    "replace > with (<|==|>=) in verify",
    # `cc_first_include_dir_providing_cached` joins a non-empty parent onto
    # the include directory; an empty parent joined is the same directory.
    "replace match guard !parent.as_os_str\\(\\).is_empty\\(\\) with true in cc_first_include_dir_providing_cached",
    # `FileHasher::prefetch` warms the daemon's hash cache and returns before
    # doing anything without a socket; the keys it later serves are the same
    # either way, only their latency differs.
    "replace FileHasher<'db>::prefetch with \\(\\)",
    # `Run::action_key` folds a `DEP_*` path's content only when the value is
    # absolute and present. The value itself is always folded, so an absent
    # or relative value keys the same with or without the extra fold.
    "replace && with \\|\\| in Run::action_key",
    # `get_cc_preprocess_memo` accepts a memo only when its recorded input
    # count equals the inputs joined in. The check runs only once a joined
    # input row exists, so the count it compares against is at least one and
    # `> 0` is the same test as `>= 0` there; an empty reference set returns
    # before the comparison and is covered directly.
    "replace > with >= in FileHashCache<'_>::get_cc_preprocess_memo",
]
